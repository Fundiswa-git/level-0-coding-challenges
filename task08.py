import determine
num = []
hours = []
  for 1 in num:
      if 1<=12:
     time=str(datetime.timedelta(hours=1-12))
  else:
      time=str(datetime.timedelta(hours=1-12))+"pm"hours.append(time)
      print(hours)
      